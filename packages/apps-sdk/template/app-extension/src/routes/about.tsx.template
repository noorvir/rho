import { useRhoApp } from "@rho/apps-sdk/react";
import { Link, Screen } from "@rho/ui";

export function About() {
	const rho = useRhoApp();

	return (
		<Screen title="About __APP_NAME__" description={`Mounted at ${rho.app.basePath}.`}>
			<Link className="text-sm underline underline-offset-4" href="/">
				Back home
			</Link>
		</Screen>
	);
}
